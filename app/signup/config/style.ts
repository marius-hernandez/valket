export const style={
    "main":[
        'h-screen grid grid-cols-1 grid-co', //sm
        'lg:grid-cols-2' //lg
    ],
    "image":[
        'm-auto', //sm
        '' //lg
    ],
    
}

export const main = (style.main.join(' '))
export const image = (style.image.join(' '))
